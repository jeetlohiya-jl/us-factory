# Copy to .env for local dev, or set these as real environment variables in
# your hosting platform (Render/Railway/Fly/etc). See README.md for the full
# explanation of each adapter and how to switch it to the real Supabase-backed
# implementation.

# --- Database -----------------------------------------------------------
# Local dev default. In production, point this at your Supabase Postgres
# connection string (Project Settings -> Database -> Connection string ->
# "Session pooler" or "Direct connection" — NOT "Transaction pooler").
FACTORY_DATABASE_URL=postgresql+psycopg2://factory_app:factory_dev_pw@localhost:5432/factory_os

# --- Storage --------------------------------------------------------------
# "local" (filesystem, dev default) or "supabase"
FACTORY_STORAGE_PROVIDER=local
FACTORY_LOCAL_STORAGE_DIR=./storage_data
# Required only when FACTORY_STORAGE_PROVIDER=supabase:
FACTORY_SUPABASE_URL=
FACTORY_SUPABASE_SERVICE_KEY=
FACTORY_SUPABASE_STORAGE_BUCKET=inward-vehicle-inspection

# --- Auth -------------------------------------------------------------------
# "dev" (static bearer-token users, default) or "supabase" (real Google OAuth
# via Supabase Auth)
FACTORY_AUTH_PROVIDER=dev
# When FACTORY_AUTH_PROVIDER=supabase, tokens are verified against the
# project's public JWKS endpoint (derived from FACTORY_SUPABASE_URL above)
# -- no shared secret needed here. (There used to be a FACTORY_SUPABASE_JWT_SECRET
# var for the older HS256 "Legacy JWT Secret" signing mode; it's no longer
# read, since it doesn't work for projects on the newer asymmetric signing keys.)

# --- OCR ------------------------------------------------------------------
# "tesseract" (real, local, default) or "none"
FACTORY_OCR_PROVIDER=tesseract
# On Linux, the tesseract-ocr / poppler-utils system packages put these
# binaries on PATH automatically -- these two are normally left blank there.
# On Windows, the Tesseract-OCR and poppler installers do NOT add themselves
# to PATH, so photo OCR (Inward Vehicle Inspection) and COA PDF/image parsing
# (Inward QC) will fail with "Tesseract not found" / "poppler not installed"
# until you either add both to your System PATH, or set these two instead:
#   FACTORY_TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe
#   FACTORY_POPPLER_PATH=C:\poppler-24.x\Library\bin
FACTORY_TESSERACT_CMD=
FACTORY_POPPLER_PATH=

# --- CORS -------------------------------------------------------------------
# JSON array of allowed frontend origins.
FACTORY_CORS_ORIGINS=["http://localhost:3000"]
